# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# data / uploads (runtime state)
/data/*.db
/data/*.db-*
/data/*.json

# client-provided source material (photo archives are far past GitHub's limit)
*.rar
*.zip
/public/uploads/*
!/public/uploads/.gitkeep

# env
.env
.env*.local

# GCP service-account keys — NEVER commit
/usacars-*.json
/secrets/
*-*.iam.gserviceaccount.json

# Server backups (contain .env secrets + DB) — NEVER commit
/backups/

# testing
/coverage

# misc
*.tsbuildinfo
next-env.d.ts.timestamp
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
